export const links = {
  home: { path: "/", text: "home" },
  playground: { path: "/playground", text: "playground" },
  resume: { path: "/resume", text: "resume" },
}

export enum colors {
  blue = "#0E184F",
  white = "#FFFFFF",
}

export enum zIndex {
  content = 0,
  background = content - 1,
}
