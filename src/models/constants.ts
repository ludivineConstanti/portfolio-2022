export const links = {
  home: { path: "/", text: "home" },
  playground: { path: "/playground", text: "playground" },
  resume: { path: "/resume", text: "resume" },
}

export enum colors {
  blue = "#1e2a6b",
  // backgroundText = "#1e2a6bBF",
  white = "#FFFFFF",
}

export enum zIndex {
  content = 0,
  background = content - 1,
}
